.env
__pycache__/
*.pyc
logs/
facturador.log
reintentos.json
resumenes.json

# Lo genera el instalador con las rutas de cada PC. Versionarlo hacia que
# una instalacion nueva heredara las rutas de otra maquina y PM2 fallara.
sfs.config.js

# Artefactos de compilacion del instalador. Se distribuyen por release, no aca.
instalacion/FacturadorSetup/
instalacion/FacturadorSetup.zip
instalacion/_build/

# Datos del contribuyente de cada instalacion. No lleva claves, pero si el RUC,
# la direccion fiscal y la ruta al certificado de un cliente concreto.
cliente.conf
